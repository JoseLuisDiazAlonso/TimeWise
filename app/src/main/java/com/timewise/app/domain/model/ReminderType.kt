package com.timewise.app.domain.model

/**
 * La función de este enum es la de determinar el origen del recordatorio para poder usar
 * en el widget un icono distinto o un formato distanto en el widget.
 */

enum class ReminderType {
    TASK,
    TIMEBLOCK
}
