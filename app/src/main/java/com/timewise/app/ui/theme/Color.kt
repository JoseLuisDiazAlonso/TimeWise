package com.timewise.app.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales de marca
val TimeWisePrimary = Color(0xFF6200EE)
val TimeWisePrimaryDark = Color(0xFF3700B3)
val TimeWiseSecondary = Color(0xFF03A9F4)

// Colores de superficie y fondo (modo claro)
val LightBackground = Color(0xFFFAFAFA)
val LightSurface = Color(0xFFFFFFFF)
val LightOnSurface = Color(0xFF1C1B1F)

// Colores de superficie y fondo (modo oscuro)
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkOnSurface = Color(0xFFE6E1E5)

// Colores semánticos para la prioridad de las tareas
val PriorityHigh = Color(0xFFE53935)
val PriorityMedium = Color(0xFFFB8C00)
val PriorityLow = Color(0xFF43A047)

// Colores de estado
val SuccessColor = Color(0xFF43A047)
val ErrorColor = Color(0xFFE53935)

// Colores semánticos para las categorías

val CategoryWork    = Color(0xFF1565C0)  // Trabajo  — Azul 800
val CategoryPersonal = Color(0xFFE65100) // Personal — Naranja 800
val CategoryStudy   = Color(0xFF283593)  // Estudio  — Índigo 800
val CategoryHealth  = Color(0xFF00897B)  // Salud    — Teal 600 (ya existe)
val CategoryHome    = Color(0xFF2E7D32)  // Casa     — Verde 800
val CategoryNeutral = Color(0xFF79747E)  // Neutro   (ya existe)